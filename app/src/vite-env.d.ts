/// <reference types="vite/client" />

/** AudioWorklet entry bundled by the `audio-worklet` plugin in vite.config.ts. */
declare module "*?audio-worklet" {
  const url: string;
  export default url;
}
